name: Ejecutar main.py

on:
  push:
    branches:
      - main

jobs:
  ejecutar_main_py:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Ejecutar main.py
        run: python main.py
